# Minimum versions — kept loose so pip installs a prebuilt wheel for your
# platform/Python instead of trying to compile from source (which needs a C
# compiler). pip will pick the newest compatible release.

# Core data + image handling
numpy>=1.26
pydicom>=2.4
nibabel>=5.1
scipy>=1.11
scikit-image>=0.22
pillow>=10.0

# Web app
flask>=3.0

# Desktop viewer
matplotlib>=3.8
